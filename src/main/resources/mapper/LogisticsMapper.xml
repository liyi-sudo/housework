<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.mapper.LogisticsMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO logistics (order_id, logistics_no, company, status, trace)
        VALUES (#{orderId}, #{logisticsNo}, #{company}, #{status}, #{trace})
    </insert>

    <select id="selectByOrderId" resultType="Logistics">
        SELECT * FROM logistics WHERE order_id = #{orderId} LIMIT 1
    </select>
</mapper>