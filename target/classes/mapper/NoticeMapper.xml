<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.mapper.NoticeMapper">
    <select id="selectShowList" resultType="Notice">
        SELECT * FROM notice WHERE status = 1 ORDER BY sort ASC, create_time DESC
    </select>
    <select id="selectById" resultType="Notice">
        SELECT * FROM notice WHERE id = #{id}
    </select>

    <select id="selectPage" resultType="Notice">
        SELECT * FROM notice
        <where>
            <if test="keyword != null and keyword != ''">AND title LIKE CONCAT('%', #{keyword}, '%')</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(*) FROM notice
        <where>
            <if test="keyword != null and keyword != ''">AND title LIKE CONCAT('%', #{keyword}, '%')</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notice (title, content, status, sort)
        VALUES (#{title}, #{content}, #{status}, #{sort})
    </insert>

    <update id="update">
        UPDATE notice SET title = #{title}, content = #{content}, status = #{status}, sort = #{sort}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE notice SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM notice WHERE id = #{id}
    </delete>
</mapper>