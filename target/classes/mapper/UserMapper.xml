<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.mapper.UserMapper">

    <resultMap id="Base" type="User">
        <id property="id" column="id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByPhone" resultMap="Base">
        SELECT * FROM user WHERE phone = #{phone}
    </select>

    <select id="selectById" resultMap="Base">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="selectByOpenid" resultMap="Base">
        SELECT * FROM user WHERE openid = #{openid} LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (phone, password, nickname, avatar, gender, status)
        VALUES (#{phone}, #{password}, #{nickname}, #{avatar}, #{gender}, 1)
    </insert>

    <update id="updateInfo">
        UPDATE user SET nickname = #{nickname}, avatar = #{avatar}, gender = #{gender} WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE user SET password = #{password} WHERE id = #{id}
    </update>

    <update id="updateOpenid">
        UPDATE user SET openid = #{openid} WHERE id = #{id}
    </update>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM user
    </select>

    <select id="selectPage" resultType="User">
        SELECT * FROM user
        <where>
            <if test="keyword != null and keyword != ''">
                AND (phone LIKE CONCAT('%', #{keyword}, '%') OR nickname LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(*) FROM user
        <where>
            <if test="keyword != null and keyword != ''">
                AND (phone LIKE CONCAT('%', #{keyword}, '%') OR nickname LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>

    <update id="updateStatus">
        UPDATE user SET status = #{status} WHERE id = #{id}
    </update>
</mapper>