# process —— 三段联调后端总处理逻辑说明

> 所属模块：process
> 技术栈：Spring Boot
> 说明：本目录存放四个前端端（user_pc / user_mini / merchant / manager）联调时背后的**总后端处理逻辑**，非功能渲染页面。

---

## 1. 总体架构
- Spring Boot 单后端（多模块），统一服务四个前端端
- 模块划分：

| 模块 | 职责 |
|---|---|
| common | 统一返回体、全局异常、工具类 |
| auth | Token 鉴权、登录日志 |
| user | 用户端接口（user_pc / user_mini 共用） |
| mall | 商品、购物车、订单、支付、物流 |
| merchant | 商家后台接口 |
| admin | 管理后台接口 |
| process | 订单状态机、入驻审核流程 |
| job | 定时任务 |

## 2. 统一规范
- 统一返回结构 `{code, message, data}`
- 全局异常处理、参数校验
- RESTful 接口 + 统一分页
- **Token 鉴权（JWT）**：签发 / 校验 / 过期；登录日志统一写入

## 3. RBAC 权限体系
- 三种角色：用户 / 商家 / 管理员
- 商家数据隔离：所有商家接口强制校验店铺归属
- 接口按端分组，跨端越权访问拦截

## 4. 核心业务处理
- **订单状态机**：
  ```
  待支付 → 待发货 → 待收货 → 已完成
            （取消 / 售后中 分支）
  ```
- **库存并发控制**：下单锁定库存、支付确认扣减、超时释放（乐观锁）
- **入驻审核流程**：待审核 → 通过 / 驳回，状态流转留痕
- **模拟支付**：支付流水生成、支付成功回调（模拟）、超时未支付自动取消
- **模拟物流**：发货生成物流单、确认收货推进状态

## 5. 定时任务
- 超时未支付自动取消
- 超时未收货自动完成
- 库存预警计算

## 6. 数据模型总览
核心表：用户、商家、店铺、分类、商品 / SKU、购物车、订单 / 订单项、支付流水、物流单、评价、售后单、优惠券 / 用户券、公告、登录日志、审批记录、操作日志

## 7. 接口清单（按端给出）
- 分段列出 user_pc / user_mini / merchant / manager 各自所需接口表（路径、方法、鉴权、说明），随详细设计细化